// import { INotificationProvider } from "../interfaces/notification";
// import { sendNotif } from "./firebaseNotification";

// export class FirebaseNotificationProvider implements INotificationProvider {
//   async sendNotification(user?: string, message?: string, data?: any): Promise<void> {
//     const {title, body, token} = data
//     const toFireBase = sendNotif(token, title, body)
//   }
// }
